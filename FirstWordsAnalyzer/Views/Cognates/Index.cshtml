@model IEnumerable<FirstWordsAnalyzer.Models.Cognate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WordPart)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WrongAssociation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Word.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Word1.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WordPart)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WrongAssociation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Word.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Word1.Text)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
